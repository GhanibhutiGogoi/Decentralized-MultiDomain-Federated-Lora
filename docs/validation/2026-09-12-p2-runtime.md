# Project 2 runtime validation — 12 September 2026

Starting revision: `8c22e76` (main). All Python test execution for these changes used the isolated SSH snapshot `ahlora-review-20260912-8c22e76` on the documented GPU host.

- Full P2 suite: **100 passed, 46 subtests passed**, 3.31 seconds.
- Python 3.10.12; PyTorch 2.3.0+cu121; torchvision 0.18.0; torchaudio 2.3.0; torchtext 0.18.0; torchdata 0.8.0; portalocker 2.10.1; soundfile 0.12.1.
- Test command: `PYTHONPATH=~/pystubs OMP_NUM_THREADS=2 python -m pytest tests/ -q --no-header -p no:warnings -rf`, from Project 2 with the remote virtual environment's Python.
- Real-data preflight: FashionMNIST 60,000/10,000; AG News 120,000/7,600; heart disease 237/60. AG News shares its training vocabulary with the test split. No synthetic fallback or downloads occurred during these preflights.
- The initially attempted torchdata 0.7.1 failed to import with PyTorch 2.3 (`DILL_AVAILABLE` incompatibility); 0.8.0 passed the real AG News loading path and is the committed pin.

The tests cover paired update-space rank projection, retained singular values, heterogeneous effective scaling, repeated communication without artificial shrinkage, and audio metadata extraction without decoding every waveform. The shared-alpha ResNet server contract remains explicit. SpeechCommands evaluation follows the existing official validation subset.

This validation does not rehabilitate historical Experiment 1/2 outputs. Regenerated scientific data and calibration are recorded separately in the completion artifacts.
