# Project 2 Plan

## Redesign Objective

Project 2 is being redesigned as a clean, modular architecture for domain-aware allocation research.

## Why Project 2 Is Redesigned

The redesign separates the new Project 2 structure from earlier exploratory code so the repository can grow from a simple, well-scoped foundation.

## Project 1 Utilities To Reuse

Project 1 utilities expected to be reused later include shared training helpers, dataset handling utilities, model support code, evaluation helpers, and result management patterns.

No Project 1 code is copied in this initialization commit.

## Repository Initialization

This commit creates only the folder skeleton, package markers, placeholder experiment entry point, top-level configuration placeholder, and project documentation.

## High-Level Roadmap

1. Confirm the package boundaries for federated logic, source models and datasets, allocation modules, aggregation, lambda allocation, experiments, and results.
2. Identify the exact Project 1 utilities to reuse.
3. Port reusable utilities into the new architecture.
4. Add experiment scaffolding after the architecture is confirmed.
5. Implement research logic in later commits.
